const INIT_SEED = 1234;
const EVENT_NUM = 5;
const GUEST_NUM = 200;
const POLL_NUM = 20;
const CANDIDATE_NUM = 110; // 이 값은 자동생성되는 candidates의 예상 갯수보다 약간 작은 수임
const VOTE_NUM = 100;

const config = {
	INIT_SEED,
	EVENT_NUM,
	GUEST_NUM,
	POLL_NUM,
	CANDIDATE_NUM,
	VOTE_NUM,
};

export default config;
