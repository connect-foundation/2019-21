type Event {
	id: Int!
	code: String!
	moderationOption: Boolean!
	replyOption: Boolean!
	createdAt: String!
	updatedAt: String!
	endAt: String!
	HostId: ID!
}

type Query {
	events(hostId: ID!): [Event]!
}
