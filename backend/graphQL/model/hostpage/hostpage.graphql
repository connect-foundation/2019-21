type Event {
	id: Int!
	eventCode: String!
	moderationOption: Boolean!
	replyOption: Boolean!
	createdAt: String!
	updatedAt: String!
	endAt: String!
	HostId: ID!
}

type Host {
	id: Int!
	oauthId: ID!
	name: String!
	email: String!
	emailFeedBack: Boolean!
	image: String!
}

type InitData {
	events: [Event]!
	host: Host!
}

type Query {
	events: [Event]!
	init: InitData!
}

type Mutation{
    moderation(eventId:Int!, moderationOption:Boolean!): Int!
}
