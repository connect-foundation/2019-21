type Question {
	id: ID!
	EventId: ID!
	GuestId: ID!
	QuestionId: ID
	createdAt: String!
	content: String!
	state: String!
	isStared: Boolean!
	likeCount: Int!
}

type Query {
    questions(EventId:ID!): [Question]!,
},